diff --git a/src/index.ts b/src/index.ts
index b20f34f..3ff5e32 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -1392,13 +1392,19 @@ export class Minimatch {
   }
 
   slashSplit(p: string) {
+    const isAbsoluteDrive = /^[a-zA-Z]:[/\\]/.test(p)
+
     // if p starts with // on windows, we preserve that
     // so that UNC paths aren't broken.  Otherwise, any number of
     // / characters are coalesced into one, unless
     // preserveMultipleSlashes is set to true.
     if (this.preserveMultipleSlashes) {
       return p.split('/')
-    } else if (this.isWindows && /^\/\/[^/]+/.test(p)) {
+    } else if (
+      this.isWindows &&
+      !isAbsoluteDrive &&
+      /^\/\/[^/]+/.test(p)
+    ) {
       // add an extra '' for the one we lose
       return ['', ...p.split(/\/+/)]
     } else {
diff --git a/test/win-path-sep.js b/test/win-path-sep.js
index 984e223..89f2cdf 100644
--- a/test/win-path-sep.js
+++ b/test/win-path-sep.js
@@ -45,3 +45,10 @@ t.test('override with options', async t => {
 
   t.end()
 })
+
+t.test('drive-letter absolute paths bypass UNC preprocessing', async t => {
+  process.env.__MINIMATCH_TESTING_PLATFORM__ = 'win32'
+  const { minimatch: mm } = await t.mockImport('../dist/esm/index.js', {})
+  t.equal(mm('c:/temp/file', 'c:/*'), true)
+  t.end()
+})
